/* ═══════════════════════════════════════════════════════════
   COLLÈGE ASIMOV — Configuration
   USE_API = true  → API REST locale (V2)
   USE_API = false → Supabase (V1)
   ═══════════════════════════════════════════════════════════ */
const CONFIG = {
  USE_API:  true,
  API_URL:  'http://localhost:3000/api',
};
 